# Core module

Cross-cutting infrastructure only: navigation, localization, theme primitives, reusable widgets, shared models, connectivity, persistence, and platform adapters.

Feature modules must not reach into another feature's private implementation.