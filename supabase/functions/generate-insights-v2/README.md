# Professional insight contract

The function returns JSON containing `summary`, `summaryBullets`, `actionItems`, `deadlines`, and `mentionedPeople`. Vocabulary and themes are intentionally not generated or persisted.